#include "main.h"

/**
 * _isupper - checks for uppercase letter
 * @x: number to be checked
 *
 * Return: 1 for uppercase letter or 2 for otherwise
 */

int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
